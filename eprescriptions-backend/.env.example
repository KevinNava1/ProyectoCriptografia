# ─── MySQL ───
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=rxuser
DB_PASSWORD=Changeme1!
DB_NAME=securerx

# ─── JWT ───
# OBLIGATORIO. Genera uno aleatorio con:
#   openssl rand -hex 32
# El backend RECHAZA al arranque cualquier valor placeholder (incluyendo el
# de este ejemplo). Mínimo 32 chars con entropía real.
JWT_SECRET=
JWT_EXPIRE_MINUTES=60

# ─── CA interna (material persistente) ───
CA_DIR=ca

# ─── CORS (whitelist separada por comas) ───
# nginx termina TLS 1.3 en https://localhost y proxea a api:8000.
# El frontend Vite dev sigue corriendo en http://localhost:5173 → ambos en la lista.
CORS_ORIGINS=https://localhost,http://localhost:5173,http://127.0.0.1:5173

# ─── Admin bootstrap (idempotente — se crea SOLO si no hay admin activo) ───
# En contenedores, setea ADMIN_PASSWORD y el admin se genera al arranque.
# El bundle PEM privado se imprime UNA sola vez en los logs — guárdalo.
ADMIN_USERNAME=admin
ADMIN_NOMBRE=Administrador
ADMIN_EMAIL=admin@securerx.app
ADMIN_PASSWORD=
