# Python
__pycache__/
*.py[cod]
venv/
*.egg-info/
.pytest_cache/

# Env
.env

# Material criptográfico — NUNCA subir
ca/                # CA interna (priv y cert root, on-first-boot)
admin_keys/        # bundle PEM del admin generado por bootstrap_admin
certs/             # cert auto-firmado de nginx (TLS 1.3 local)
*.pem
*.key

# IDE
.vscode/
.idea/